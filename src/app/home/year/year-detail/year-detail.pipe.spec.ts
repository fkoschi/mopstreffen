import { YearDetailPipe } from './year-detail.pipe';

describe('YearDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new YearDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
